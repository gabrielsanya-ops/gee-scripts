# Forest

Sub-scripts for **users/gabrielsanya/Forest** in the GEE Code Editor.

Add one `.js` file per script: open each script in [code.earthengine.google.com](https://code.earthengine.google.com/), copy the code, and save it here with the same name (e.g. `MyScript.js`).

## Scripts

| Script | Description |
| --- | --- |
| `Coffee_builtup.js` | Supervised classification using Google Satellite Embedding v1 + Sentinel-2 predictors; trains GBDT, validates accuracy, renders legend |
